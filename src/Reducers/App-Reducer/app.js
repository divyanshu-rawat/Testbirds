

import * as t from '../../ActionTypes';
import data from '../../DataSet/data.json'

const app = (state = data , action) => {

	switch(action.type){
		default:
			return state
	}
}

export default app